name: vello_backend
description: AI assistant backend for Vello app
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  http: ^1.2.1
  dotenv: ^4.2.0

dev_dependencies:
  lints: ^3.0.0
  